{
  "extends": "./tsconfig.json",
  "exclude": [
    "node_modules",  // Already excluded
    "test",           // Already excluded
    "dist",           // Already excluded
    "**/*.spec.ts"    // Excludes any other spec files
  ]
}
